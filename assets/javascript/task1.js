/* Put code for the task below */
const button = document.query('button');
const img = document.query('img');
let display;
function buttonHovered() {
    if (buttonHovered) {
        button.style.display = "block";
    }

}
function buttonClicked() {
    if(buttonClicked) {
        img.style.display = "none";
    }
}